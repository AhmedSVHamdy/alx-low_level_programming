#include <stdio.h>

/**
 * main - Prints the alphaber.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  char apl[26] = "abcdefghjklm";
  int i;
  for (i = 0; i < 26; i++)
  {
	putchar(alp[i]);
  }
        putchar('\n');
      	return (0);
}	
